---
all:
  children:
    # ─── Plateforme NOC/SOC ─────────────────────────────────────────────────────
    noc_platform:
      hosts:
        noc-server-01:
          ansible_host: 213.199.42.187
          ansible_user: abdoukarimdiouf
          ansible_port: 22
          ansible_ssh_private_key_file: ~/.ssh/id_rsa

    # ─── Serveurs Linux (groupés par hébergeur) ────────────────────────────────
    linux_servers:
      children:
        # ── Contabo ──────────────────────────────────────────────────────────
        contabo_servers:
          hosts:
            SRVTEK06:
              ansible_host: 62.84.182.32
              ansible_port: 1243
              site: contabo
            srvtek04:
              ansible_host: 62.84.182.30
              ansible_port: 1243
              site: contabo
            srvtek05:
              ansible_host: 62.84.182.31
              ansible_port: 1243
              site: contabo
            srvdevil:
              ansible_host: 80.241.215.160
              ansible_port: 1243
              site: contabo
            srvgitlab:
              ansible_host: 149.102.157.141
              ansible_port: 1243
              site: contabo
            srvakeema:
              ansible_host: 77.237.235.109
              ansible_port: 1243
              site: contabo
        # ── OVH ─────────────────────────────────────────────────────────────
        ovh_servers:
          hosts:
            # srv-ovh-01:
            #   ansible_host: 51.X.X.X
            #   ansible_port: 1243
            #   site: ovh
        # ── Hetzner ──────────────────────────────────────────────────────────
        hetzner_servers:
          hosts:
            # srv-hetzner-01:
            #   ansible_host: 78.X.X.X
            #   ansible_port: 1243
            #   site: hetzner

    # ─── Équipements réseau ─────────────────────────────────────────────────────
    cisco_devices:
      hosts:
        # core-router-01:
        #   ansible_host: 192.168.1.1
        #   ansible_network_os: ios
        # core-router-02:
        #   ansible_host: 192.168.1.2
        #   ansible_network_os: ios
        # dist-switch-01:
        #   ansible_host: 192.168.1.10
        #   ansible_network_os: ios
        # dist-switch-02:
        #   ansible_host: 192.168.1.11
        #   ansible_network_os: ios
        # firewall-01:
        #   ansible_host: 192.168.1.254
        #   ansible_network_os: asa

  vars:
    ansible_user: abdoukarimdiouf
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
    snmp_auth_password: "{{ vault_snmp_auth_password }}"
    snmp_priv_password: "{{ vault_snmp_priv_password }}"
